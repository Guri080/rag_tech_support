remake_embeddings = False # set true to recreatea all vector embeddings for similarity search
base_llm = "gpt-4.1-mini" # LLM to for proving weights to candiate chunk and rag system
debug_rag = False # prints the pipelines output to CLI. Includes model's output from contradiction detector